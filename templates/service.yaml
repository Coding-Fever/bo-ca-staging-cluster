apiVersion: v1
kind: Service
metadata:
  name: django-svc
  labels:
    app: django-svc
  annotations:
    cloud.google.com/backend-config: '{"default": "django-backend-config"}'
spec:
  type: ClusterIP      
  selector:
    app: django-svc
  ports:
    - port: 80
      targetPort: 8000
